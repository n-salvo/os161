{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "startKernel",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/../root"
            },
            "command": "sys161 -w kernel & sleep 4"
        }
    ]
}